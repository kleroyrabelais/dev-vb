Module ex94
    Sub main()

        Dim represantant As String
        Dim semaineplus As Integer = 1
        Dim VenteHT As Double
        Dim tsemaine As Integer = 1
        Console.WriteLine("Nom du représentant")
        represantant = Console.ReadLine()
        Console.WriteLine("Semaine " + tsemaine + ": Ventes  H.T hebdomadaires (0 pour stopper la saisie)")
        VenteHT = Console.ReadLine()

    End Sub
End Module
